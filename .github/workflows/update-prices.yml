name: Auto Update Gold Prices

on:
  schedule:
    # Chạy mỗi 15 phút từ 7h sáng đến 11h đêm (giờ VN = UTC+7)
    - cron: '0,15,30,45 0-16 * * *'  # 7:00, 7:15... đến 23:45 VN
  workflow_dispatch:  # Cho phép chạy thủ công

permissions:
  contents: write

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Quan trọng để git push không lỗi
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: |
          npm install
          # Cài đặt trình duyệt cho Puppeteer
          npx puppeteer browsers install chrome
          # Cài đặt các thư viện cần thiết cho hệ thống (Debian/Ubuntu)
          sudo apt-get update
          sudo apt-get install -y libgbm-dev libnss3 libatk-bridge2.0-0 libx11-xcb1 libxcb-dri3-0 libxcomposite1 libxcursor1 libxdamage1 libxfixes3 libxi6 libxrandr2 libxrender1 libxtst6 libcups2 libdrm2 libxkbcommon0 libasound2
        
      - name: Fetch gold prices
        run: node fetch-prices.js
        
      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add prices.js
          
          # Kiểm tra xem có thay đổi thực sự không
          if [ -n "$(git status --porcelain)" ]; then
            git commit -m "🔄 Cập nhật giá vàng tự động [Skip CI] - $(TZ='Asia/Ho_Chi_Minh' date '+%d/%m/%Y %H:%M')"
            # Kéo về trước khi đẩy để tránh xung đột
            git pull --rebase origin main
            git push origin main
          else
            echo "Không có thay đổi."
          fi
